<html>
<head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
<%- include('includes/navbar', {links: links}) %>
<h1 class="title"><%= title %></h1>
<main>
        <div>
            <p><%=product.product %></p>
            <p><%=product.type%></p>
            <p><%=product.manufacturer%></p>
            <p><%=product.price%></p>
        </div>
    <form action=/products/delete/<%=product.id%> method="POST" style="display:inline;">
        <button type="submit" onclick="return confirm('Are you sure you want to delete this item?');">Delete item</button>
    </form>
</main>
</body>
</html>